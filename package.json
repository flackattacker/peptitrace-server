{
  "name": "server",
  "version": "1.0.0",
  "description": "PeptiTrace API Server",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node --max-old-space-size=4096 index.js || cat /root/.npm/_logs/*.log",
    "poststart": "cat /root/.npm/_logs/*.log",
    "dev": "nodemon --max-old-space-size=4096 index.js",
    "build": "echo 'No build step required for server'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.6.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
